SELECT
    SUM(b.SCORE) AS SCORE, a.EMP_NO, a.EMP_NAME, a.POSITION, a.EMAIL 
FROM
    HR_EMPLOYEES AS a
JOIN
    HR_GRADE AS b
ON
    a.EMP_NO=b.EMP_NO
WHERE
    b.YEAR='2022'
GROUP BY
    a.EMP_NO,
    a.EMP_NAME,
    a.POSITION,
    a.EMAIL
ORDER BY
    SCORE DESC
LIMIT 1

